    IF BoolTagTrue%(mess$, "MOUSERBDOWN") THEN
        'The right mousebutton got pressed, consider the button is still
        'down until receiving a "MOUSERBUP" event. The current button
        'state is also monitored in tag "MOUSERB", which is delivered
        'with all messages.
    END IF

